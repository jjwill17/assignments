import React from 'react'

const Footer = () => {
    const styles = {
        backgroundColor: "white",
        marginTop: "0",
        marginBottom: "0",
        padding: "25px 25px 25px 25px",
        textAlign: "center",
        borderRadius: "0 40px 0 0",
        backgroundImage: "radial-gradient(circle at -18px 36px, rgba(0, 0, 0, 0) 0, rgba(0, 0, 0, 0) 39px, forestgreen 40px)"
    }
    return (
        <div style={styles}>Footer</div>
    )
}

export default Footer